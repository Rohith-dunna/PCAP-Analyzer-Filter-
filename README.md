# PCAP-Analyzer-Filter-
A Python-based PCAP analyzer and packet filtering tool built using Scapy. It allows users to analyze .pcap and .pcapng files and filter network packets based on IP addresses, ports, and protocols such as TCP, UDP, ICMP, and DNS.
